# Local dev (default): SQLite file in backend/ — no extra services required
DATABASE_URL=sqlite+aiosqlite:///./dclaw_doc.db
# Production / CI: Postgres
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/dclaw_doc

APP_ENV=dev
BACKEND_PORT=8107
FRONTEND_PORT=3020
NEXT_PUBLIC_API_URL=http://localhost:8107

# Optional — AI providers (used in Complexity 1)
# OPENROUTER_API_KEY=
# OLLAMA_URL=http://localhost:11434
